#############################################################################
# File templated by Ansible
# Base template: {{ template_path }}
# User: {{ template_uid }} on {{ template_host }}
#############################################################################

# This should be installed by ansible as /etc/iptables.rules and reloaded
# automatically by a handler when it is changed.

# WARNING
# Be careful when editing this file not to restrict ssh access. Doing that will
# render the host unreachable in a very broken way.

*filter 
:INPUT DROP [1:242]
:FORWARD DROP [0:0] 
:OUTPUT DROP [0:0]

# Only accept packets which are part of a properly formed connection.
-A INPUT -m state --state RELATED,ESTABLISHED -j ACCEPT 

# Allow traffic for link-local addresses.
-A INPUT -s fe80::/10 -j ACCEPT

# Accept ssh, http, and https traffic.
-A INPUT -p tcp -m tcp --dport 22 -j ACCEPT
-A INPUT -p tcp -m tcp --dport 80 -j ACCEPT
-A INPUT -p tcp -m tcp --dport 443 -j ACCEPT

# And anything over the loopback interface.
-A INPUT -p all -i lo -j ACCEPT 

# Allow incoming and outgoing ICMPv6 traffic.
-A INPUT -p icmpv6 -j ACCEPT
-A OUTPUT -p icmpv6 -j ACCEPT

# Any packets claiming to be from localhost but coming from an external
# interface are evil.
-A INPUT -p all -s localhost -i eth0 -j DROP

# Only allow outgoing packets which are part of a properly formed connection.
-A OUTPUT -m state --state RELATED,ESTABLISHED -j ACCEPT 

# Restrict outbound traffic to legit stuff: ssh, smtp, dns, http, ntp, https,
# submission, mssql, git.
-A OUTPUT -p tcp -m tcp --dport 22 -j ACCEPT 
-A OUTPUT -p tcp -m tcp --dport 25 -j ACCEPT
-A OUTPUT -p udp -m udp --dport 53 -j ACCEPT 
-A OUTPUT -p tcp -m tcp --dport 80 -j ACCEPT 
-A OUTPUT -p udp -m udp --dport 123 -j ACCEPT
-A OUTPUT -p tcp -m tcp --dport 443 -j ACCEPT 
-A OUTPUT -p tcp -m tcp --dport 587 -j ACCEPT 
-A OUTPUT -p tcp -m tcp --dport 1433 -j ACCEPT 
-A OUTPUT -p tcp -m tcp --dport 9418 -j ACCEPT 
-A OUTPUT -p all -o lo -j ACCEPT 

COMMIT

<%inherit file="base.txt"/>\
An address change has been requested for order ${order.id}.

${request.route_url('admin:order', id=order.id)}

Current Address:
${self.address_fields(order.shipping)}

Desired Address:
${self.address_fields(new_address)}

IP Address: ${request.client_addr}
Browser: ${request.user_agent}
